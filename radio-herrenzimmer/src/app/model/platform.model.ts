import { Image } from './image.model';

// Repräsentiert eine Social-Media-Plattform
export class Platform {
    
    // URL der Plattform
    url: string;

    // Name der Plattform
    title: string;

    // Bild-Icon für die Darstellung
    icon: Image;
}